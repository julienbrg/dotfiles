---
description: Publish a file to Fileverse dDocs and return the shareable link
argument-hint: <filepath> [ddocId]
allowed-tools: Bash(share:*)
---

Run exactly `share $ARGUMENTS` and report the shareable link to me. (Re-sharing a file that was already published updates the existing doc — the link stays the same.)
